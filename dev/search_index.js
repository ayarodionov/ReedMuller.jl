var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#API-Reference","page":"API Reference","text":"","title":"API Reference"},{"category":"section","location":"api/#Codes","page":"API Reference","text":"","title":"Codes"},{"category":"section","location":"api/#Common-interface","page":"API Reference","text":"","title":"Common interface"},{"category":"section","location":"api/#Encoders","page":"API Reference","text":"","title":"Encoders"},{"category":"section","location":"api/#Decoders","page":"API Reference","text":"","title":"Decoders"},{"category":"section","location":"api/#Generic-decoder-wrappers","page":"API Reference","text":"","title":"Generic decoder wrappers"},{"category":"section","location":"api/#Channels","page":"API Reference","text":"","title":"Channels"},{"category":"section","location":"api/#Simulation","page":"API Reference","text":"","title":"Simulation"},{"category":"module","location":"api/#ReedMuller.ReedMuller","page":"API Reference","text":"ReedMuller\n\nA common framework for implementing and comparing Reed-Muller encoding and decoding algorithms in Julia.\n\nEvery algorithm plugs into the same interface:\n\nRMCode(r, m) describes the code RM(r, m).\nEncoders subtype AbstractEncoder and implement encode(enc, code, message::BitVector)::BitVector.\nDecoders subtype AbstractDecoder and implement decode(dec, code, llr::Vector{Float64})::BitVector (log-likelihood ratios, positive = bit 0 more likely) and basis(dec)::Symbol naming the message-coordinate convention they use (:monomial or :plotkin).\nsimulate(...) runs encoder/channel/decoder pipelines and reports bit and word error rates, so different algorithms can be compared under identical conditions.\n\n\n\n\n\n","title":"ReedMuller.ReedMuller"},{"category":"type","location":"api/#ReedMuller.RMCode","page":"API Reference","text":"RMCode(r, m)\n\nThe Reed-Muller code RM(r, m): length 2^m, dimension sum(binomial(m, i) for i in 0:r), minimum distance 2^(m-r).\n\n\n\n\n\n","title":"ReedMuller.RMCode"},{"category":"function","location":"api/#ReedMuller.blocklength","page":"API Reference","text":"blocklength(code::RMCode) -> Int\n\nCodeword length n = 2^m of code.\n\n\n\n\n\n","title":"ReedMuller.blocklength"},{"category":"function","location":"api/#ReedMuller.dimension","page":"API Reference","text":"dimension(r, m) -> Int\ndimension(code::RMCode) -> Int\n\nMessage length k = sum(binomial(m, i) for i in 0:r) of RM(r, m).\n\n\n\n\n\n","title":"ReedMuller.dimension"},{"category":"function","location":"api/#ReedMuller.minimum_distance","page":"API Reference","text":"minimum_distance(code::RMCode) -> Int\n\nMinimum Hamming distance d = 2^(m-r) of code.\n\n\n\n\n\n","title":"ReedMuller.minimum_distance"},{"category":"function","location":"api/#ReedMuller.rate","page":"API Reference","text":"rate(code::RMCode) -> Float64\n\nCode rate k / n.\n\n\n\n\n\n","title":"ReedMuller.rate"},{"category":"function","location":"api/#ReedMuller.generator_matrix","page":"API Reference","text":"generator_matrix(code::RMCode; basis=:monomial) -> BitMatrix\n\nGenerator matrix of RM(r, m) in the given message convention.\n\n:monomial — row j evaluates monomials(r, m)[j].\n:plotkin  — recursive [G(r,m-1) G(r,m-1); 0 G(r-1,m-1)] construction (u-part rows first, then v-part rows).\n\n\n\n\n\n","title":"ReedMuller.generator_matrix"},{"category":"function","location":"api/#ReedMuller.monomials","page":"API Reference","text":"monomials(r, m) -> Vector{Vector{Int}}\n\nAll monomials of degree <= r in variables x1..xm, each given as the sorted list of variable indices, ordered by degree ascending and lexicographically within a degree. This fixes the :monomial message convention: message bit j is the coefficient of monomials(r, m)[j].\n\n\n\n\n\n","title":"ReedMuller.monomials"},{"category":"type","location":"api/#ReedMuller.AbstractEncoder","page":"API Reference","text":"Abstract supertype for encoders. Implement encode(enc, code, message::BitVector)::BitVector and basis(enc).\n\n\n\n\n\n","title":"ReedMuller.AbstractEncoder"},{"category":"type","location":"api/#ReedMuller.AbstractDecoder","page":"API Reference","text":"Abstract supertype for decoders. Implement decode(dec, code, llr::Vector{Float64})::BitVector and basis(dec).\n\nThe LLR convention is llr[i] = log(P(cᵢ=0 | yᵢ) / P(cᵢ=1 | yᵢ)), i.e. positive values favour bit 0. Hard-decision decoders threshold internally (llr .< 0). Use hard_llr to feed a plain bit vector to any decoder.\n\n\n\n\n\n","title":"ReedMuller.AbstractDecoder"},{"category":"function","location":"api/#ReedMuller.encode","page":"API Reference","text":"encode(enc::AbstractEncoder, code::RMCode, message::AbstractVector) -> BitVector\n\nEncode message (a length-dimension(code) bit vector, in enc's basis) into a length-blocklength(code) codeword of code.\n\n\n\n\n\n","title":"ReedMuller.encode"},{"category":"function","location":"api/#ReedMuller.decode","page":"API Reference","text":"decode(dec::AbstractDecoder, code::RMCode, llr::AbstractVector{<:Real}) -> BitVector\n\nDecode a length-blocklength(code) vector of log-likelihood ratios into the length-dimension(code) message estimate, in dec's basis. See AbstractDecoder for the LLR sign convention; use hard_llr to decode plain bits.\n\n\n\n\n\n","title":"ReedMuller.decode"},{"category":"function","location":"api/#ReedMuller.basis","page":"API Reference","text":"basis(x) -> Symbol\n\nMessage-coordinate convention of an encoder or decoder: :monomial (coefficients of Boolean monomials, degree-ascending) or :plotkin (recursive (u, u+v) layout, u-part first). An encoder and decoder can only be compared in a pipeline when their bases match.\n\n\n\n\n\n","title":"ReedMuller.basis"},{"category":"function","location":"api/#ReedMuller.hard_llr","page":"API Reference","text":"hard_llr(bits) -> Vector{Float64}\n\nTurn a hard-decision bit vector into pseudo-LLRs (+1.0 for bit 0, -1.0 for bit 1) so hard inputs can be fed to the common decode(dec, code, llr) interface.\n\n\n\n\n\n","title":"ReedMuller.hard_llr"},{"category":"type","location":"api/#ReedMuller.MatrixEncoder","page":"API Reference","text":"MatrixEncoder(code; basis=:monomial)\n\nReference encoder: multiplies the message by a precomputed generator matrix over F₂. Works with either message basis.\n\n\n\n\n\n","title":"ReedMuller.MatrixEncoder"},{"category":"type","location":"api/#ReedMuller.PlotkinEncoder","page":"API Reference","text":"PlotkinEncoder()\n\nFast recursive (u, u+v) encoder: O(n log n) XORs, no stored matrix. Message convention is :plotkin (u-part bits first, then v-part), identical to MatrixEncoder(code; basis=:plotkin).\n\n\n\n\n\n","title":"ReedMuller.PlotkinEncoder"},{"category":"type","location":"api/#ReedMuller.ReedDecoder","page":"API Reference","text":"ReedDecoder()\n\nReed's classic majority-logic decoder (Reed, 1954). Hard-decision: LLRs are thresholded to bits, then monomial coefficients are recovered degree by degree, from degree r down to 0. The coefficient of each degree-d monomial x_S is the majority over 2^(m-d) parity checks, one per assignment of the variables outside S; after a degree is finished its contribution is subtracted from the received word.\n\nGuaranteed to correct up to ⌊(2^(m-r) - 1) / 2⌋ errors. Message convention: :monomial.\n\n\n\n\n\n","title":"ReedMuller.ReedDecoder"},{"category":"type","location":"api/#ReedMuller.FHTDecoder","page":"API Reference","text":"FHTDecoder()\n\nMaximum-likelihood soft-decision decoder for first-order codes RM(1, m) via the fast Walsh-Hadamard transform (the \"Green machine\"). Complexity O(n log n). The transform of the LLR vector is maximised in absolute value at the index whose bits are the linear coefficients; the sign gives the constant term.\n\nMessage convention: :monomial, i.e. [a₀, a₁, …, a_m] with codeword c(z) = a₀ ⊕ ⊕ᵢ aᵢ zᵢ.\n\n\n\n\n\n","title":"ReedMuller.FHTDecoder"},{"category":"type","location":"api/#ReedMuller.DumerDecoder","page":"API Reference","text":"DumerDecoder(; combine = :minsum)\n\nDumer's recursive soft-decision decoder (Dumer, \"Recursive decoding and its performance for low-rate Reed-Muller codes\", 2004). Splits RM(r, m) via the Plotkin (u, u+v) decomposition, first decoding the v-part from combined half-LLRs, then the u-part from the v-corrected sum. Leaf nodes: repetition codes (r = 0) decide by the LLR sum, rate-1 codes (r = m) by symbol-wise hard decision.\n\ncombine chooses the check-node rule for the v-branch: :minsum (sign-min approximation, the usual practical choice) or :exact (2·atanh(tanh·tanh), true LLR combining).\n\nleaves = :fht stops the recursion at first-order nodes and decodes them optimally with the fast Hadamard transform instead of recursing down to single bits (the stronger termination Dumer recommends); :bits (default) recurses all the way down.\n\nComplexity O(n log n). Message convention: :plotkin.\n\n\n\n\n\n","title":"ReedMuller.DumerDecoder"},{"category":"type","location":"api/#ReedMuller.DumerShabunovDecoder","page":"API Reference","text":"DumerShabunovDecoder(L = 8; combine = :minsum)\n\nDumer-Shabunov recursive list decoding (Dumer & Shabunov, \"Soft-decision decoding of Reed-Muller codes: recursive lists\", 2006).\n\nSame Plotkin (u, u+v) recursion as DumerDecoder, but instead of committing to one decision per node it maintains a list of up to L candidate decoding paths. Information decisions are made at the leaves — repetition nodes (r = 0) and single-position nodes (m = 0), to which rate-1 subcodes reduce recursively — where every surviving path branches into both hypotheses. Paths are ranked by the standard successive-cancellation path metric: the accumulated magnitude of leaf LLRs that the path's decisions contradict. After each branching the list is pruned back to the L best paths; the best final path is returned.\n\nWith leaves = :fht the recursion instead terminates at first-order nodes, where every path is expanded into the full list of affine codewords scored by their exact correlation with the node LLRs (the list-FHT termination of the original paper). leaves = :bits (default) recurses down to repetition/single-bit leaves.\n\nL = 1 with :bits leaves reduces to plain recursive (Dumer) decoding; growing L approaches maximum-likelihood performance at cost O(L·n·log n). Message convention: :plotkin.\n\n\n\n\n\n","title":"ReedMuller.DumerShabunovDecoder"},{"category":"type","location":"api/#ReedMuller.SidelnikovPershakovDecoder","page":"API Reference","text":"SidelnikovPershakovDecoder(; voting = :weighted)\n\nSidel'nikov-Pershakov derivative decoding for second-order codes RM(2, m) (Sidel'nikov & Pershakov, \"Decoding of Reed-Muller codes with a large number of errors\", Probl. Inf. Transm., 1992).\n\nA codeword is a quadratic Boolean function f(x) = xᵀQx ⊕ ⟨l, x⟩ ⊕ a₀, so its derivative in any direction b, D_b f(x) = f(x) ⊕ f(x ⊕ b) = ⟨Bb, x⟩ ⊕ f(b) ⊕ f(0) with B = Q + Qᵀ, is affine. The decoder:\n\nFor every nonzero direction b, forms derivative LLRs by min-sum combining and ML-decodes them in RM(1, m) with the fast Walsh-Hadamard transform, giving an estimate w_b ≈ Bb with a reliability (the FHT peak magnitude).\nRecovers each entry Bij by a reliability-weighted majority vote over all pairs (b, b ⊕ ei), whose estimates XOR to B e_i.\nPeels the recovered quadratic part off the channel LLRs and FHT-decodes the residual RM(1, m) for the affine part.\n\nvoting selects the vote combination in step 2: :weighted scales each vote by the reliability of its less reliable member, :majority counts every vote equally — the simplified plain-majority voting of Sakkour's variant of the algorithm (Sakkour, \"Decoding of second order Reed-Muller codes with a large number of errors\", ITW 2005).\n\nCorrects any pattern of up to ⌊(d-1)/2⌋ = 2^(m-3) - 1 errors and, on random errors, most patterns well beyond half the minimum distance. Complexity O(n² log n). Message convention: :monomial.\n\n\n\n\n\n","title":"ReedMuller.SidelnikovPershakovDecoder"},{"category":"type","location":"api/#ReedMuller.RPADecoder","page":"API Reference","text":"RPADecoder(; iters = 0)\n\nRecursive Projection-Aggregation decoding (Ye & Abbe, \"Recursive projection-aggregation decoding of Reed-Muller codes\", 2019), the soft (LLR) variant. Performance is close to maximum likelihood for low orders (r ≤ 3).\n\nFor RM(r, m) with r ≥ 2, each iteration:\n\nProjection: for every nonzero direction z the received LLRs are projected onto the quotient by the subspace {0, z} — the LLR of y(x) ⊕ y(x ⊕ z) per coset — which carries a codeword of RM(r-1, m-1).\nRecursion: each projection is decoded recursively; RM(1, m') is reached at the bottom and decoded optimally by FHT.\nAggregation: the LLR of every position is re-estimated as the average over all directions of ±LLR(x ⊕ z), the sign taken from the decoded projection's verdict on whether y(x) = y(x ⊕ z).\n\nIterations stop early once the hard decisions are stable; iters = 0 uses the paper's default of ⌈m/2⌉. The final hard estimate is turned back into message bits with a run of Reed's decoder. Complexity O(iters · n² log n) per recursion level. Message convention: :monomial.\n\n\n\n\n\n","title":"ReedMuller.RPADecoder"},{"category":"type","location":"api/#ReedMuller.BPDecoder","page":"API Reference","text":"BPDecoder(code; iters = 30)\n\nBelief-propagation (sum-product) decoding on the Tanner graph whose check nodes are all rows of the dual code's generator matrix — for RM(r, m) the dual is RM(m-r-1, m), giving a redundant (overcomplete) parity-check set, which helps BP on these dense codes. Flooding schedule with the exact tanh check-node rule, early exit as soon as the hard decisions satisfy every check. The final hard estimate is mapped to message bits with a run of Reed's decoder.\n\nBP is known to be a weak decoder for Reed-Muller codes (their Tanner graphs have many short cycles) — it is included as a comparison baseline and as a constituent for ensemble schemes such as AutomorphismEnsembleDecoder, which recovers the multiple-bases BP idea. Message convention: :monomial.\n\n\n\n\n\n","title":"ReedMuller.BPDecoder"},{"category":"type","location":"api/#ReedMuller.GLPDecoder","page":"API Reference","text":"GLPDecoder(code, L; perms = :pairs, combine = :minsum, leaves = :fht)\n\nGlobal-list-with-permutations decoding — recursive list decoding over an ensemble of code-preserving coordinate permutations sharing one global list, after the dtrm_glp codec of Shabunov's ecclab (github.com/kshabunov/ecclab; the technique appears in Dumer & Shabunov, \"Soft-decision decoding of Reed-Muller codes: recursive lists\", 2006, §VI).\n\nWhere AutomorphismEnsembleDecoder runs its constituent decoder independently per permutation, GLP seeds the list of a single DumerShabunovDecoder-style recursion with one path per permuted version of the received word; from then on paths descended from different permutations compete for the same L list slots, so the budget flows to whichever views of the channel output look most promising.\n\nperms selects the ensemble of permutations of the m variables (each preserves RM(r, m)); the ecclab parameter sets are provided as symbols, or pass an explicit Vector of m-element permutation vectors:\n\n:identity — no permutation (P1); equivalent to plain Dumer-Shabunov decoding.\n:cyclic   — the m cyclic shifts of the variables (Pcyclic).\n:pairs    — for every variable pair (i, j), the permutation moving i, j behind the others (PmCr, C(m,2) permutations).\n\ncombine and leaves are as in DumerShabunovDecoder. Cost is one list decoding at list size ≥ number of permutations. Message convention: :plotkin.\n\n\n\n\n\n","title":"ReedMuller.GLPDecoder"},{"category":"function","location":"api/#ReedMuller.glp_permutations","page":"API Reference","text":"glp_permutations(m, kind) -> Vector{Vector{Int}}\n\nThe ecclab permutation ensembles for GLPDecoder: :identity, :cyclic (m shifts) or :pairs (each of the C(m,2) variable pairs moved behind the others, identity included as the last pair).\n\n\n\n\n\n","title":"ReedMuller.glp_permutations"},{"category":"type","location":"api/#ReedMuller.GraphSearchDecoder","page":"API Reference","text":"GraphSearchDecoder(; iters = 64, l = 8, lbar = 8, s = 5)\n\nLocal graph-search decoding (Kamenev, \"On decoding of Reed-Muller codes using a local graph search\", IEEE Trans. Commun., 2022).\n\nThe codewords of RM(r, m) form a graph whose edges connect pairs at Hamming distance 2^(m-r) (the minimum distance); a full traversal maximizing the correlation metric M = Σᵢ (1-2cᵢ) yᵢ is exactly ML decoding (Proposition 1 of the paper). The decoder starts from the output of Dumer's recursive decoder and takes up to iters greedy steps: each step moves to an adjacent, not-yet-visited codeword — found by flipping the support of a minimum-weight codeword — and the best codeword seen is returned.\n\nAdjacent codewords are found by a greedy depth-first search down the tree of nested affine subspaces of EG(m, 2) whose leaves enumerate all minimum-weight-codeword supports (the paper's NextStepGreedy): at each tree node the child minimizing the flip penalty 2 Σ_{i∈S̃} (1-2cᵢ) yᵢ is chosen, with all children scored at once by a fast Hadamard transform, so one step costs O(n log n). The l best children of the root are tried per step; when none yields a new codeword, up to lbar extra children are tried, at most s times per decode (paper defaults: l = 8, lbar = 8, s = 5).\n\nThe paper's optional CRC-based early termination is not implemented (this package has no CRC layer); the search always runs until iters steps, no new adjacent codeword can be found, or the extra budget is exhausted. Message convention: :plotkin.\n\n\n\n\n\n","title":"ReedMuller.GraphSearchDecoder"},{"category":"type","location":"api/#ReedMuller.MLDecoder","page":"API Reference","text":"MLDecoder(code; basis = :monomial)\n\nBrute-force maximum-likelihood decoding by exhaustive correlation over all 2^k codewords — the exact reference against which every other decoder can be judged. Only practical for small codes; the constructor refuses k > 24. O(2^k · n) per decoded word.\n\n\n\n\n\n","title":"ReedMuller.MLDecoder"},{"category":"type","location":"api/#ReedMuller.AutomorphismEnsembleDecoder","page":"API Reference","text":"AutomorphismEnsembleDecoder(code, inner; size = 8, rng = Random.default_rng())\n\nAutomorphism Ensemble Decoding (Geiselhart, Elkelesh, Ebada, Cammerer & ten Brink, \"Automorphism ensemble decoding of Reed-Muller codes\",\n\n— a generic wrapper around any constituent decoder.\n\nAt construction, size permutations of the coordinates are drawn from the automorphism group of the code (the general affine group GA(m): x ↦ Ax ⊕ b with A invertible), the identity always included. Decoding runs inner on each permuted version of the channel LLRs, maps every candidate codeword back, and keeps the one with the best correlation to the channel output — so the ensemble is never worse than inner alone. Cost is size × the constituent decoder.\n\nMessage convention: that of inner.\n\n\n\n\n\n","title":"ReedMuller.AutomorphismEnsembleDecoder"},{"category":"type","location":"api/#ReedMuller.ChaseDecoder","page":"API Reference","text":"ChaseDecoder(code, inner; t = 4)\n\nChase-II decoding (Chase, \"A class of algorithms for decoding block codes with channel measurement information\", 1972) around any inner decoder. The t least reliable positions are identified and all 2^t sign-flip test patterns of the channel LLRs are decoded by inner; the candidate codeword with the best correlation to the unmodified channel output wins. Turns a hard-decision decoder (e.g. ReedDecoder) into a genuinely soft-input one at cost 2^t × inner. Message convention: that of inner.\n\n\n\n\n\n","title":"ReedMuller.ChaseDecoder"},{"category":"type","location":"api/#ReedMuller.GMDDecoder","page":"API Reference","text":"GMDDecoder(code, inner)\n\nGeneralized Minimum Distance decoding (Forney, 1966) around any soft-input inner decoder: the 2j least reliable positions are erased (LLR set to 0) for j = 0, 1, …, ⌊(d-1)/2⌋, each trial is decoded by inner, and the candidate with the best correlation to the channel output wins. Includes the no-erasure trial, so it is never worse than inner alone. Message convention: that of inner.\n\n\n\n\n\n","title":"ReedMuller.GMDDecoder"},{"category":"type","location":"api/#ReedMuller.BSC","page":"API Reference","text":"BSC(p)\n\nBinary symmetric channel with crossover probability p.\n\n\n\n\n\n","title":"ReedMuller.BSC"},{"category":"type","location":"api/#ReedMuller.BIAWGN","page":"API Reference","text":"BIAWGN(sigma)\n\nBinary-input AWGN channel: BPSK modulation x = 1 - 2c, received y = x + sigma·N(0,1), LLR = 2y/sigma². For code rate R, Eb/N0 [dB] = 10·log10(1 / (2·R·sigma²)).\n\n\n\n\n\n","title":"ReedMuller.BIAWGN"},{"category":"function","location":"api/#ReedMuller.BIAWGN_from_ebn0","page":"API Reference","text":"BIAWGN_from_ebn0(ebn0_db, code) -> BIAWGN\n\nConstruct the channel from Eb/N0 in dB for the given code's rate.\n\n\n\n\n\n","title":"ReedMuller.BIAWGN_from_ebn0"},{"category":"function","location":"api/#ReedMuller.transmit","page":"API Reference","text":"transmit(rng::AbstractRNG, ch, codeword::BitVector) -> Vector{Float64}\n\nPass codeword through channel ch (a BSC or BIAWGN) and return the resulting log-likelihood ratios (positive = bit 0 more likely), ready for any AbstractDecoder.\n\n\n\n\n\n","title":"ReedMuller.transmit"},{"category":"function","location":"api/#ReedMuller.simulate","page":"API Reference","text":"simulate(enc, dec, code, channel; trials = 10_000, rng = Random.default_rng())\n\nMonte-Carlo comparison harness: random message → encode → transmit → decode, counting message bit and block errors. Throws if the encoder and decoder use different message bases (their pipelines would not be comparable).\n\n\n\n\n\n","title":"ReedMuller.simulate"},{"category":"type","location":"api/#ReedMuller.SimResult","page":"API Reference","text":"SimResult\n\nOutcome of a simulate run: message bit error rate ber, block (word) error rate wer, and the number of trials.\n\n\n\n\n\n","title":"ReedMuller.SimResult"},{"category":"section","location":"","page":"Home","text":"using Markdown\nMarkdown.parse_file(joinpath(@__DIR__, \"..\", \"..\", \"README.md\"))","title":"Home"}]
}
